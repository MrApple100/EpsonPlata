<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/mobile_navigation"
    app:startDestination="@id/navigation_send">

    <fragment
        android:id="@+id/navigation_receive"
        android:name="com.pedro.sample.live.FragmentReceive"
        android:label="FragmentReceive"
        tools:layout="@layout/fragment_receive"/>
    <fragment
        android:id="@+id/navigation_send"
        android:name="com.pedro.sample.FragmentSend"
        android:label="FragmentSend"
        tools:layout="@layout/fragment_send"/>
</navigation>